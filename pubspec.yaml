name: med_tracker
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # Firebase Core (required)
  firebase_core: ^4.1.1
  
  # Firebase Auth & Firestore
  firebase_auth: ^6.1.0
  cloud_firestore: ^6.0.2
  
  # Google Sign-In
  google_sign_in: ^7.2.0
  
  # Android Intent (for opening settings)
  android_intent_plus: ^6.0.0

  # Local Notifications
  flutter_local_notifications: ^19.4.2
  timezone: ^0.10.0

  # Utilities
  intl: ^0.18.1



dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  
  assets:
    - assets/